- `m` command for commit details does not work
- don't like that in the commit view, the commits are rendered as folders.
  Should be a sectioned header instead, and display the full commit title, wrapped
- text should be selectable in the view pane
- difft coloring means we lose syntax highlighting - would prefer to keep this,
  matching delta. Color should be background, not the text.
